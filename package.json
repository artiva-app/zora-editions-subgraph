{
  "name": "zora-editions",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service neokry/zora-editions",
    "create-local": "graph create --node http://localhost:8020/ zora-editions",
    "remove-local": "graph remove --node http://localhost:8020/ zora-editions",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zora-editions"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
