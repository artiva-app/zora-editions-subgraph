{
  "name": "zora-editions",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service neokry/nft-editions-mainnet",
    "create-local": "graph create --node http://localhost:8020/ nft-editions",
    "remove-local": "graph remove --node http://localhost:8020/ nft-editions",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nft-editions"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
